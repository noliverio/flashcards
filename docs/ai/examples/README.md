# Examples & audit logs

Store recorded agent runs and their artifacts here for review and audits. Each example should include:

- The original prompt used.
- The full agent transcript (messages exchanged).
- The produced patch/diff and list of changed files.
- Verification commands and results.
- A short human review note (who approved, when).

Use the `ai-log-template.md` to create new example logs.
